/* eslint-disable max-len */
import chai from 'chai'
import * as sandbox from './index'

chai.should()

describe('[SANDBOX]', () => {
  it('foo()', (done) => {
    sandbox.foo()
    done()
  })

  it('merge([...], [...])', (done) => {
    // Assertions.

    done()
  })
})
