/* eslint-disable no-unused-expressions, no-console */
import * as heaps from './heaps'

describe('[Data Structure] Heaps', () => {
  it('', () => {
    // Assertions
    expect(1).toEqual(1)
  })
})
