/* eslint-disable no-unused-expressions */
import { sum, maxSums, closeDuplicates } from './fixed-size'

describe.skip('[Data Structures] Sliding Window → Variable size', () => {
  describe.skip('dunno', () => {
    it.skip('also dunno', () => {
      // const err = "Cannot read properties of null (reading 'slice')"
      // expect(() => { sum(null) }).toThrow(err)
    })
  })
})
